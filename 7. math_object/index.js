// Cung cấp các phương thức và thuộc tính của nhiều giá trị trong TOÁN học


console.log(Math.PI)

let x = 3;
let y = 2;
let a = 10;
let z;

// z = Math.round(x); // round có nghĩa sẽ làm tròn số lớn nhất nếu đằng sau là số lớn hơn 5 thì sẽ tăng còn nhỏ hơn thì giữ nguyên
// z = Math.floor(x);  // floor sẽ luôn đưa trở về số ban đầu 
// z = Math.ceil(x); // ceil sẽ luôn làm tròn đến số lớn hơn
// z = Math.trunc(x); 
// z = Math.pow(x, y); // có nghĩa là x mũ y  (3^2)
// z = Math.sqrt(x); 
// z = Math.log(x);
// z = Math.sin(x); 
// z = Math.cos(x); 
// z = Math.tan(x); 
// z = Math.abs(x); 
// z = Math.sign(x); 

let max = Math.max(x,y,a);
let min = Math.min(x,y,a)

console.log(z);

